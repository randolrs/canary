<%= form_for(@gallery_submission) do |f| %>
  <% if @gallery_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gallery_submission.errors.count, "error") %> prohibited this gallery_submission from being saved:</h2>

      <ul>
      <% @gallery_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :gallery_id %><br>
    <%= f.number_field :gallery_id %>
  </div>
  <div class="field">
    <%= f.label :collection_id %><br>
    <%= f.number_field :collection_id %>
  </div>
  <div class="field">
    <%= f.label :additional_description %><br>
    <%= f.text_area :additional_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
