<% unless user_signed_in? %>

	<%= render 'pages/basic_landing_page' %>

<%else%>
	
	<% if current_user.is_artist %>
	
		<%= render 'pages/artist_dashboard' %>

	<% elsif current_user.is_buyer %>

		<%= render 'pages/buyer_dashboard' %>

	<%end%>

<%end%>